import { createBrowserRouter } from 'react-router-dom'
import StatisticsPage from './index'

export const statisticsRouter = createBrowserRouter([
  {
    path: '/data/statistics',
    element: <StatisticsPage />,
  },
])
